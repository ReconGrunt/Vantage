{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the Vantage main window.",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default"]
}
