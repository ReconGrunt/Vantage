{
  "name": "livelysky",
  "version": "0.1.0",
  "description": "LivelySky — a live planetarium dome: real aircraft, satellites, planets, weather and stars projected at their true sky positions.",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=20"
  }
}
